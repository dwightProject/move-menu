const getters = {
  test(state) {
    return state.test
  },
  pop(state) {
    return state.pop
  },
  mask(state) {
    return state.mask
  },
  loading(state) {
    return state.loading
  },
  ip(state) {
    return state.ip
  },
  whichname(state) {
    return state.whichname
  },
};

export default getters;
